name: Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to VPS via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: '180.93.136.93'
          username: 'root'
          password: '#wF?*tyPs^j4'
          script: |
            cd /var/www/blog
            git fetch --all
            git reset --hard origin/main
            git clean -fd
            npm install
            rm -rf .astro dist node_modules/.astro node_modules/.vite
            npm run build
            npm install -g pm2
            PORT=3001 pm2 restart blog --update-env || PORT=3001 pm2 start dist/server/entry.mjs --name blog
            systemctl reload nginx
